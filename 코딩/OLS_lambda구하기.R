x1 <- 1:10
x2 <- 2:11
x2[c(2,4,7)] <- c(4,6,9)
y <- c(3,5,5,10,11,12,14,17,19,20)

X <- cbind(1, x1, x2)

OLS <- solve(t(X) %*% X) %*% t(X) %*% y # 일반선형회귀분석의 w 추정값
lambda <- 100000
RD <- solve(t(X) %*% X + lambda*diag(3)) %*% t(X) %*% y # 일반선형회귀분석의 w 추정값
RD
